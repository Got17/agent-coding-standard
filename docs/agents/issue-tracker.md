# Issue Tracker

GitHub Issues in `Got17/agent-coding-standard`.

## Rules for AI agents

1. Use the `gh` CLI for all issue operations (`gh issue list`, `gh issue view`, `gh issue create`, `gh issue comment`).
2. Always inspect existing issues before creating a new one to prevent duplicates.
3. Include clear labels, reproduction steps, or context when filing issues.
